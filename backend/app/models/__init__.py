# ==============================================================================
# models/__init__.py - Models Package Initializer
# ==============================================================================
# This file makes the 'models' folder a Python package.
# When Python sees an __init__.py file in a folder, it treats that folder
# as a package that can be imported (e.g., 'from app.models.user import User').
#
# This file is intentionally minimal - the actual model definitions are in
# their own files (user.py, transaction.py, budget.py, etc.).
# ==============================================================================
