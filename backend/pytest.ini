[pytest]
# Tell pytest where to find tests
testpaths = tests

# Show verbose output by default
addopts = -v --tb=short

# Treat these as test file patterns
python_files = test_*.py
python_classes = Test*
python_functions = test_*
